#include "gui_lib.hpp"

namespace gui
{
    sf::RenderWindow* window_ptr;
    /*Context context;

    void Context::init(sf::Vector2f size, const char* window_name)
    {
        window_ptr = std::make_unique<sf::RenderWindow>(sf::VideoMode(size.x, size.y), window_name);
    }

    sf::RenderWindow Context::window()
    {
        return *window_ptr;
    }*/
}